/* Component scaffolds — placeholder.

   As you port each component from design_reference/, drop it into a folder here
   (e.g. components/Card/Card.tsx + Card.module.css) and re-export from an
   index.ts. The shells in shell/desktop and shell/web import from here.

   Recommended structure per component:

     components/Card/
       Card.tsx              ← the component
       Card.module.css       ← scoped styles
       index.ts              ← `export { Card } from './Card';`

   This file exists only so the directory shows up in version control. Delete
   it when the first real component lands. */

export {};
